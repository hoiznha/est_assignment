# FastAPI 및 서버
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# 임베딩 모델
sentence-transformers>=2.2.0
# PyTorch는 Dockerfile에서 CPU-only 버전으로 별도 설치
# (CUDA 제거로 이미지 크기 대폭 감소)

# 벡터 데이터베이스
chromadb>=0.4.0

# 데이터 처리
pandas>=2.0.0
openpyxl>=3.1.0  # 엑셀 파일 읽기용

# 기타 유틸리티
python-multipart>=0.0.6  # FastAPI 파일 업로드용



